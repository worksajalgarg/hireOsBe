# Lightweight LiveKit Cloud configuration (< 100MB RAM). This is what
# Dockerfile actually installs for the voice_agent worker image — confirmed
# app/voice_agent/worker.py never imports anything from app/agents/parsing,
# so none of the resume/JD parsing stack (docling, torch, pypdfium2 — see
# requirements-api.txt) belongs in this file. Split out from a single
# requirements.txt that used to also install the API's ~500MB+ docling/torch
# dependencies into this same worker image even though the worker never
# used them — see requirements-api.txt's comment for the parsing side.
fastapi==0.139.2
starlette>=1.3.1
uvicorn[standard]==0.51.0
pydantic==2.13.4
PyYAML==6.0.3
python-dotenv==1.2.2
openai==2.49.0
google-genai==2.14.0
livekit-agents==1.6.7
livekit-plugins-deepgram==1.6.7
livekit-plugins-elevenlabs==1.6.7
