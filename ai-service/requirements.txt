# Alias, not the source of truth — kept so existing workflows that install
# from this exact filename (requirements-dev.txt's `-r requirements.txt`,
# any external docs/scripts) keep working unchanged. The real split is
# requirements-worker.txt (what Dockerfile actually installs for the voice
# worker — < 100MB RAM) vs requirements-api.txt (adds the ~500MB+
# docling/torch resume/JD parsing stack, worker never needs it) — see
# those two files' comments for why they're separate.
-r requirements-api.txt
